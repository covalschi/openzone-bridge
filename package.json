{
  "name": "openzone-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Connects a DayZ server running OpenZone to a Discord guild. The bot's SQLite is the home of chat, news and roles; Discord and the game mirror it.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/discord.js && node --check src/store.js && node --check src/http.js && node --check src/oauth.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5"
  },
  "license": "CC-BY-NC-SA-4.0"
}
