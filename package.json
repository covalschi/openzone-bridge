{
  "name": "openzone-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Connects a DayZ server running OpenZone to a Discord guild. Discord is the source of truth; the game mirrors it.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/discord.js && node --check src/store.js && node --check src/http.js && node --check src/oauth.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5"
  },
  "license": "CC-BY-NC-SA-4.0"
}
